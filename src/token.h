#ifndef TOKEN_H
#define TOKEN_H

typedef enum {
	TOKEN_EOF,

	TOKEN_LPAREN,
	TOKEN_RPAREN,
	TOKEN_LBRACE,
	TOKEN_RBRACE,
	TOKEN_LBRACKET,
	TOKEN_RBRACKET,

	TOKEN_PERCENT,
	TOKEN_ASTERISK,
	TOKEN_PLUS,
	TOKEN_MINUS,
	TOKEN_SLASH,
	TOKEN_CARET,
	TOKEN_EQUALS,

	TOKEN_COMMA,
	TOKEN_COLON,
	TOKEN_SEMICOLON,

	TOKEN_DECREMENT,
	TOKEN_INCREMENT,

	TOKEN_EQ,
	TOKEN_NE,
	TOKEN_LT,
	TOKEN_LE,
	TOKEN_GT,
	TOKEN_GE,

	TOKEN_AND,
	TOKEN_OR,

	TOKEN_ARRAY,
	TOKEN_BOOLEAN,
	TOKEN_CHAR,
	TOKEN_ELSE,
	TOKEN_FALSE,
	TOKEN_FOR,
	TOKEN_FUNCTION,
	TOKEN_IF,
	TOKEN_INTEGER,
	TOKEN_PRINT,
	TOKEN_RETURN,
	TOKEN_STRING,
	TOKEN_TRUE,
	TOKEN_VOID,
	TOKEN_WHILE,

	TOKEN_IDENTIFIER,

	TOKEN_INTEGER_LITERAL,
	TOKEN_CHARACTER_LITERAL,
	TOKEN_STRING_LITERAL
} token_t;

#endif

